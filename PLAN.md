# libgen-cli 重构与优化计划

本项目针对 Library Genesis 镜像源反爬策略漂移、高延迟检索、单测脆弱及内嵌依赖臃肿等痛点，通过强模型（Advisor）深度架构审计，制定渐进式重构路线图。

---

## 阶段一：核心链路修复与瘦身（已完成，Commit: `4ad7893`）

- [x] **同源 Referer 补全**：修复 `ads.php` 因防盗链缺失返回 0 字节空包的致命问题，打通 CDN 直链解析。
- [x] **两阶段并发拉取元数据**：
  - 采用 4 协程并发查询 `object=f` 文件级信息。
  - 对 `object=e` 实施分批合并请求（每批最多 30 个 ID），检索网络往返次数由 `1 + 2n` 降至 `1 + ceil(n/4) + ceil(n/30)`。
- [x] **剥离 Kubo 依赖**：移除嵌入式 Kubo IPFS 节点与 DHT 模块，转为标准 HTTP 公共网关（Cloudflare、ipfs.io）流式读取，`go.sum` 依赖精简 99%。
- [x] **原子写入与文件名安全**：
  - 下载流先写入目标目录 `.tmp` 临时文件，校验成功后原子重命名（Rename），防止残留截断损坏文件。
  - 自动清洗书名中的非法字符（如 `/`、`\` 等），避免误解析为多级目录导致写盘失败。
- [x] **并发 Status 与边界修复**：
  - `status` 命令改为多镜像并发探测，耗时由 60 秒降至 5 秒内。
  - 消除备用镜像重试中的死循环逻辑与切片越界 panic。

---

## 阶段二：解析器稳健化与下载候选链（下一阶段）

- [ ] **DOM 树解析替代正则表达式**：
  - 引入 `golang.org/x/net/html` 遍历解析 `<a>` 标签。
  - 依据请求返回的有效 BaseURL 动态拼接相对路径，摆脱对多变前端 HTML 标记的正则脆弱依赖。
- [ ] **Candidate Ladder 抽象**：
  - 建立统一的下载候选对象（`Candidate{Kind, URL, Referer, PageURL}`）。
  - 按优先级降级执行：`Preferred Mirror ads.php` -> `file.php` 备用源 -> `External Gateways`。
- [ ] **严格区分直链与落地页**：
  - 分离直接可下载的二进制流（DirectHTTP / Gateway）与网页跳转页（LandingPage / Anna's Archive），禁止将网页内容当作书籍写入。

---

## 阶段三：CLI 规范契约与本地测试基建（待启动）

- [ ] **Cobra 错误传递迁移**：
  - 子命令全面由 `Run` 迁移至 `RunE`。
  - 清理业务逻辑内部直接调用的 `os.Exit` 与 `log.Fatal`，由 Cobra 根节点统一拦截与返回退出码。
- [ ] **标准流与机器交互隔离**：
  - `stdout` 仅输出结构化数据与最终文件信息，便于 Agent 和自动化管道消费。
  - 状态提示、警告与下载进度条统一引流至 `stderr`。
- [ ] **基于 httptest 的本地测试集**：
  - 剥离对外部实时网络与镜像存活状态的强依赖。
  - 编写本地 HTTP Mock 服务，固化防盗链验证、批量 ID 合并、断网降级与异常响应等单元测试用例。
